---
export interface Props {
  examples: string[];
  tag: string
}

const { examples, tag } = Astro.props;
---

<ul>
    {examples.map((example) => (
        <li>
            {example} {tag}
        </li>
    ))}
</ul>
