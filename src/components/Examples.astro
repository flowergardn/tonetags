---
export interface Props {
  examples: string[];
}

const { examples, tag } = Astro.props;
---

<ul>
    {examples.map((example, index) => (
        <li>
            {example} {tag}
        </li>
    ))}
</ul>
